# CannaSpot v3.6 - Configuration Template
# Copy this to .env and set your production values

# Flask Configuration
SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_STRING_IN_PRODUCTION

# Database (SQLite default, or use MySQL)
# SQLite: (leave commented for default)
# DATABASE_URL=sqlite:///cannaspot.db

# MySQL:
# DATABASE_URL=mysql+pymysql://username:password@localhost/cannaspot_db

# === SMTP Email Configuration (REQUIRED for user verification & password reset) ===
# Gmail Setup:
#   1. Enable 2-Factor Authentication in your Google Account
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Generate an "App Password" for CannaSpot
#   4. Use that password below (NOT your regular Gmail password)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password-here
SMTP_FROM=CannaSpot <your-email@gmail.com>
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# Other Email Providers:
# - Outlook/Hotmail: smtp-mail.outlook.com, port 587, TLS
# - Yahoo: smtp.mail.yahoo.com, port 587, TLS
# - Custom: Check your provider's SMTP settings

# Upload Limits
MAX_CONTENT_LENGTH=536870912  # 512MB in bytes

# Server Configuration (for production)
HOST=0.0.0.0
PORT=5000
